---
name: Release

on:
  push:
    tags:
      - '[0-9]*'

jobs:
  publish:
    uses: ./.github/workflows/_publish.yml
    with:
      version: ${{ github.ref_name }}
    permissions:
      contents: write
      id-token: write
