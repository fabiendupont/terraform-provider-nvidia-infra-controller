---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "nico_tenant_identity Data Source - nico"
subcategory: ""
description: |-
  Tenant identity (JWT-SVID issuance) and RFC 8693 token delegation.
---

# nico_tenant_identity (Data Source)

Tenant identity (JWT-SVID issuance) and RFC 8693 token delegation.

## Example Usage

```terraform
data "nico_tenant_identity" "example" {
  id = "resource-uuid"
}

output "tenant_identity_name" {
  value = data.nico_tenant_identity.example.name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `site_id` (String) Path parameter: site_id.

### Read-Only

- `allowed_audiences` (List of String) Stored allowlist of audience strings. Always non-empty: when a PUT supplied an empty list, the Core gRPC API substituted `[defaultAudience]` before persisting. Issuance rejects audiences outside this list.
- `created` (String) Date/time when the Tenant identity configuration was created
- `default_audience` (String) Default audience used for Tenant identity tokens
- `enabled` (Boolean) Whether Tenant identity token delegation is enabled
- `issuer` (String) Issuer URL for Tenant identity tokens
- `org` (String) Organization that owns the Tenant identity configuration
- `signing_keys` (Attributes List) Per-org signing keys currently published in JWKS. Exactly one entry has `currentSigner: true`. During a rotation overlap window a second entry is present with `currentSigner: false` and a populated `expireAt`; once the overlap window elapses the Core gRPC API deletes the expired entry and only the current signer remains. (see [below for nested schema](#nestedatt--signing_keys))
- `subject_prefix` (String) SPIFFE ID prefix used in the JWT `sub` claim. When the PUT body omitted `subjectPrefix`, Core stored `spiffe://<trust-domain-from-issuer>` here, so the value returned by GET may differ from what was submitted.
- `token_ttl_seconds` (Number) Lifetime of issued Tenant identity tokens, in seconds
- `updated` (String) Date/time when the Tenant identity configuration was last updated

<a id="nestedatt--signing_keys"></a>
### Nested Schema for `signing_keys`

Read-Only:

- `alg` (String) Signing algorithm, e.g. `ES256`.
- `current_signer` (Boolean) True when this key is the active signer for new JWT-SVIDs. Exactly one entry per response is `true`.
- `expire_at` (String) Set on the inactive (previous) key during the JWKS overlap window; the Core gRPC API deletes the inactive slot once `now >= expireAt`. Null on the current signer.
- `kid` (String) Key identifier; matches the `kid` published in JWKS for this key.
