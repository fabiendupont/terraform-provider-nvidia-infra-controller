---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "nico_ipxe_template Data Source - nico"
subcategory: ""
description: |-
  iPXE Templates are read-only definitions propagated from nico-core into the REST API. Each template has a stable core UUID and a globally unique name; per-site availability is tracked in IpxeTemplateSiteAssociation records rather than on the template row itself. List and retrieve operations return templates reported at sites the caller is authorized for (optionally filtered by siteId). Templated iPXE Operating Systems reference a template by ipxeTemplateId.
---

# nico_ipxe_template (Data Source)

iPXE Templates are read-only definitions propagated from nico-core into the REST API. Each template has a stable core UUID and a globally unique name; per-site availability is tracked in IpxeTemplateSiteAssociation records rather than on the template row itself. List and retrieve operations return templates reported at sites the caller is authorized for (optionally filtered by `siteId`). Templated iPXE Operating Systems reference a template by `ipxeTemplateId`.

## Example Usage

```terraform
data "nico_ipxe_template" "example" {
  id = "resource-uuid"
}

output "ipxe_template_name" {
  value = data.nico_ipxe_template.example.name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) ID of the resource to retrieve. When set, returns a single resource.
- `site_id` (String) Optional site ID(s); may be repeated to restrict results to templates available at any of the sites

### Read-Only

- `created` (String) created attribute.
- `name` (String) Globally unique template name
- `required_artifacts` (List of String) Artifact names required for the template
- `required_params` (List of String) Parameters that must be provided to render the template
- `reserved_params` (List of String) Parameters reserved by the template and not user-supplied
- `template` (String) Raw iPXE script content
- `updated` (String) updated attribute.
- `visibility` (String) Template visibility: Internal or Public
